# Extensions

Put runtime TypeScript extension files here before running the installer.

Examples:

```text
desktop.ts
internet.ts
memory.ts
```

The installer copies this directory to:

```text
$ALIGNED_AGENT_HOME/agent/extensions/
```

This folder intentionally contains no private harness code by default.
